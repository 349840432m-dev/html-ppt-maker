# 视觉系统

- 气质：
- 图形语言：
- 图片风格：
- 禁用视觉：（默认包含：科技蓝紫渐变、无主题抽象背景）
- HTML 动效原则：
- PPT 转场原则：

## 六项基础设计契约

- 层级（标题/结论、正文、注释/来源的字号与字重关系）：
- 留白（安全边距、模块 gap、计划保留的空白区及其作用）：
- 色彩（中性色系、唯一主强调色、第二语义色的允许场景）：
- 图文对齐（共用网格、边缘/中心线/基线、图与文字各自职责）：
- 数据聚焦（每个数据页的唯一结论、关键数字数量、来源策略）：
- 全篇节奏（重复 token、版式变化频率、视觉锚点间隔）：

## 色彩 token（写入 CSS 变量，不要在版式里写死色值）

- 页面底色 `--bg`：
- 面板色 `--bg-raise`：
- 主文字 `--ink`：
- 次级文字 `--ink-2`：
- 弱化文字 `--ink-3`：
- 主强调色 `--accent`：
- 对比强调色 `--accent-warm`：
- 深色页底 `--dark-bg`：
- 深色页文字 `--dark-ink` / `--dark-ink-2`：

## 字体策略

- 中文标题字体与气质（默认衬线制造正式感）：
- 中文正文字体与气质：
- 数字字体（默认 `tabular-nums`）：
- 离线降级链：

## 版式节奏

- 使用的版式清单（对应模板 `layout-*` 类名）：
- 深色页出现位置（章节页/结尾页）：
- 每章的节奏变化页：
